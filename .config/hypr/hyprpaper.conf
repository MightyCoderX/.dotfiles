$bg_path = ~/.dotfiles/wallpapers/current

preload = $bg_path

wallpaper {
    monitor =
    path = $bg_path
    fit_mode = cover
}

#wallpaper = monitor2,/path/to/next_image.png

#enable splash text rendering over the wallpaper
splash = true

#fully disable ipc
# ipc = off
